// Place your settings in this file to overwrite default and user settings.
{
  "files.exclude": {
    // Set to true to hide the "out" folder with the compiled JS files
    "out": false
  },
  "search.exclude": {
    // Set to false to include "out" folder in search results
    "out": true
  },
  // Use TS server from node_modules folder to control its version
  "typescript.tsdk": "./node_modules/typescript/lib"
}
